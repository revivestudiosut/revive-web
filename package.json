{
  "name": "revive-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:dev": "npm run build && npx wrangler pages deploy dist --project-name revive-web --branch dev",
    "deploy:staging": "npm run build && npx wrangler pages deploy dist --project-name revive-web --branch staging",
    "deploy:production": "npm run build && npx wrangler pages deploy dist --project-name revive-web --branch main"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^6.1.4",
    "tailwindcss": "^3.4.19"
  }
}
