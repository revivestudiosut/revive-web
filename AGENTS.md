# Agents Guide

This project's documentation lives in [`CLAUDE.md`](./CLAUDE.md). See it for the project overview, tech stack, design system, build/deploy steps, and environment setup.

Key reminder: **build and deployment always go through the `justfile` recipes** (`just build`, `just deploy-dev`, `just deploy-production`, etc.), never raw `npm` or `wrangler`.
